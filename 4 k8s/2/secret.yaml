  # secrets are applied before everything else
  apiVersion: v1
  kind: Secret
  metadata:
    name: mongodb-secret        
  type: Opaque
  data:
  # all values are base64 encoded using (echo -n "value" | base64)
    username: bWFuYW4= 
    password: bWFuYW4=